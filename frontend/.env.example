# Optional. The Vite dev server (`bun run dev`) reads MAPBOX_TOKEN from the
# repo-root .env, the same file the backend and bundled UI use, so this file is
# only needed to override it for the dev server.
MAPBOX_TOKEN=pk.your_token_here
