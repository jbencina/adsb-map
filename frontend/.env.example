# Tokens only. Copy to `frontend/.env` for the dev server (`bun run dev`).
# When the UI is served by `adsb serve` or `adsb ui`, the token comes from
# MAPBOX_TOKEN in that process's environment (or its .env) instead.
VITE_MAPBOX_TOKEN=pk.your_token_here
