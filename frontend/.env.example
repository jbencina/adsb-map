# Required for the dev server (`bun run dev`).
# In production (bundled wheel), set MAPBOX_TOKEN as a server env var on `adsb serve` instead.
VITE_MAPBOX_TOKEN=abc123

# Optional. Leave unset to use Vite's /api proxy (recommended in dev).
# VITE_API_URL=http://localhost:8000
