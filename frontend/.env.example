# Tokens only. Copy to `frontend/.env` for the Vite dev server (`bun run dev`).
# The bundled UI (`adsb start frontend`) reads MAPBOX_TOKEN from its own
# environment or .env instead.
VITE_MAPBOX_TOKEN=pk.your_token_here
